#loadingscreen
	position absolute
	left 0
	right 0
	top 0
	bottom 0
	opacity 0
	z-index -1
	transition all 1s ease-in-out
	overflow hidden
	& * 
		transition all 0.5s ease-in-out

#loadingscreen
	@import "shade"
	@import "aside"
	@import "article"

#loadingscreen.active
	z-index 99
	opacity 1
